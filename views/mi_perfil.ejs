<!doctype html>
<html lang="es" data-bs-theme="light">

<head>
  <meta charset="utf-8" />
  <title>Mi perfil | Inventario & Mantenimientos HSRT</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

  <style>
    body {
      font-family: "Inter", sans-serif;
      background-color: var(--bs-body-bg);
      transition: background-color .3s ease, color .3s ease;
    }

    /* ------ SIDEBAR ------ */
    .sidebar {
      width: 240px;
      height: 100vh;
      position: fixed;
      left: 0;
      top: 0;
      background: var(--bs-dark-bg-subtle);
      border-right: 1px solid var(--bs-border-color);
      padding-top: 1rem;
      transition: background-color .3s;
    }

    .sidebar .logo {
      font-size: 1.5rem;
      font-weight: 6000;
      text-align: center;
      padding-bottom: 1rem;
    }

    .sidebar .nav-link {
      padding: 0.7rem 1.2rem;
      font-size: 0.92rem;
      display: flex;
      gap: 10px;
      align-items: center;
      border-radius: 0.45rem;
      transition: background-color .2s;
    }

    .sidebar .nav-link:hover {
      background-color: rgba(90, 126, 255, 0.15);
    }

    .sidebar .nav-link.active {
      background-color: #4e73df;
      color: white !important;
      font-weight: 600;
    }

    /* ------ MAIN CONTENT ------ */
    .content {
      margin-left: 240px;
      padding: 1.8rem;
      min-height: 100vh;
    }

    .page-title {
      font-size: 1.6rem;
      font-weight: 700;
    }

    /* ------ NAVBAR SUPERIOR ------ */
    .topbar {
      height: 60px;
      background: var(--bs-body-bg);
      border-bottom: 1px solid var(--bs-border-color);
      display: flex;
      justify-content: flex-end;
      align-items: center;
      padding: 0 1.5rem;
      position: fixed;
      left: 240px;
      right: 0;
      top: 0;
      z-index: 10;
    }

    /* ------ TARJETA ------ */
    .card {
      border: none;
      border-radius: 0.7rem;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.07);
      transition: background-color .3s;
    }

    /* ------ TABLA ------ */
    .table thead th {
      background-color: var(--bs-tertiary-bg);
      font-size: 0.78rem;
      text-transform: uppercase;
      letter-spacing: .04em;
    }

    .table tbody tr:hover {
      background-color: rgba(80, 80, 255, 0.06);
      transition: background-color .2s;
    }

    /* ------ SWITCH MODO OSCURO ------ */
    .switch-theme {
      display: flex;
      align-items: center;
      gap: 8px;
      cursor: pointer;
      font-size: 1rem;
    }

    footer {
      margin-left: 240px;
    }
  </style>

</head>

<body>

  <!-- SIDEBAR -->
  <aside class="sidebar">
    <div class="logo mb-3">
      <img src="/img/logo.png" style="height:40px" class="mb-2 rounded"><br>
UNIVERSIDAD SANTO TOMAS
    </div>

    <nav class="nav flex-column px-2">
      <a href="/equipos" class="nav-link"><i class="bi bi-hdd"></i> Equipos</a>
      <a href="/ubicaciones" class="nav-link"><i class="bi bi-geo-alt"></i> Ubicaciones</a>
      <a href="/responsables-custodios" class="nav-link"><i class="bi bi-people"></i> Responsables</a>
      <a href="/personas-mantenimiento" class="nav-link"><i class="bi bi-person-badge"></i> Personas</a>
      <a href="/cargos" class="nav-link active"><i class="bi bi-briefcase"></i> Cargos</a>
      <a href="/mantenimientos" class="nav-link"><i class="bi bi-wrench"></i> Mantenimientos</a>
      <% if (currentUser && currentUser.role==='admin' ) { %>
        <a href="/usuarios" class="nav-link"><i class="bi bi-shield-lock"></i> Usuarios</a>
        <% } %>
          <a href="/mi-perfil" class="nav-link"><i class="bi bi-person-circle"></i> Mi perfil</a>
    </nav>
  </aside>

  <!-- TOPBAR -->
  <div class="topbar">
    <div class="switch-theme me-4">
      <input type="checkbox" id="themeToggle">
      <label for="themeToggle" class="m-0">🌙</label>
    </div>

    <% if (currentUser) { %>
      <span class="text-muted me-3 small">
        <strong>
          <%= currentUser.username %>
        </strong> | <%= currentUser.role %>
      </span>

      <form method="post" action="/logout">
        <button class="btn btn-outline-danger btn-sm">Salir</button>
      </form>
      <% } %>
  </div>

  <main class="content">
    <div class="container py-4">
      <div class="page-wrapper">
        <div class="row justify-content-center">
          <div class="col-md-7 col-lg-6">

            <div class="card profile-card bg-white">
              <div class="card-header bg-white border-0 border-bottom">
                <div class="profile-header">
                  <div class="profile-avatar">
                    <span>👤</span>
                  </div>
                  <div>
                    <h5 class="mb-1">Mi perfil</h5>
                    <small class="text-muted">
                      Actualiza tu usuario y, si lo deseas, cambia tu contraseña de acceso.
                    </small>
                  </div>
                </div>
              </div>

              <div class="card-body">
                <form id="formPerfil">
                  <div class="mb-3">
                    <label class="form-label">Usuario</label>
                    <input type="text" class="form-control" id="username" value="<%= user.username %>" required>
                    <div class="form-text">
                      Este es tu nombre de usuario para ingresar al sistema.
                    </div>
                  </div>

                  <div class="mb-3">
                    <label class="form-label">Nueva contraseña</label>
                    <input type="password" class="form-control" id="password"
                      placeholder="Deje en blanco para mantener la actual">
                    <div class="form-text">
                      Si decides cambiarla, debe tener al menos 6 caracteres.
                    </div>
                  </div>

                  <div class="mb-3">
                    <label class="form-label">Rol asignado</label>
                    <input type="text" class="form-control" value="<%= user.role %>" disabled>
                    <div class="form-text">
                      Este rol define los permisos que tienes dentro del módulo.
                    </div>
                  </div>

                  <div class="alert alert-danger d-none" id="errorBox"></div>
                  <div class="alert alert-success d-none" id="successBox"></div>

                  <div class="d-flex justify-content-end gap-2">
                    <a href="/" class="btn btn-outline-secondary btn-sm">Volver al inicio</a>
                    <button type="submit" class="btn btn-primary btn-sm">
                      Guardar cambios
                    </button>
                  </div>
                </form>
              </div>
            </div>

          </div>
        </div>
      </div>
    </div>
  </main>

  <footer class="mt-auto py-2 border-top bg-white text-center text-body-secondary small" style="margin-left: 240px;">
    © <%= new Date().getFullYear() %> USTA – Módulo de gestión de inventario y registro de mantenimientos
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/mi_perfil.js"></script>

  <script>
    const toggle = document.getElementById("themeToggle");
    const root = document.documentElement;

    const saved = localStorage.getItem("theme");
    if (saved) {
      root.setAttribute("data-bs-theme", saved);
      toggle.checked = saved === "dark";
    }

    toggle.addEventListener("change", () => {
      const theme = toggle.checked ? "dark" : "light";
      root.setAttribute("data-bs-theme", theme);
      localStorage.setItem("theme", theme);
    });
  </script>
</body>

</html>