<!doctype html>
<html lang="es" data-bs-theme="light">

<head>
  <meta charset="utf-8" />
  <title>Inicio | Inventario & Mantenimientos</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

  <style>
    body {
      font-family: "Inter", sans-serif;
      background-color: var(--bs-body-bg);
      transition: background-color .3s ease, color .3s ease;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* ------ SIDEBAR ------ */
    .sidebar {
      width: 240px;
      height: 100vh;
      position: fixed;
      left: 0;
      top: 0;
      background: var(--bs-dark-bg-subtle);
      border-right: 1px solid var(--bs-border-color);
      padding-top: 1rem;
      transition: background-color .3s;
    }

    .sidebar .logo {
      font-size: 1.5rem;
      font-weight: 6000;
      text-align: center;
      padding-bottom: 1rem;
    }

    .sidebar .nav-link {
      padding: 0.7rem 1.2rem;
      font-size: 0.92rem;
      display: flex;
      gap: 10px;
      align-items: center;
      border-radius: 0.45rem;
      transition: background-color .2s;
    }

    .sidebar .nav-link:hover {
      background-color: rgba(90, 126, 255, 0.15);
    }

    .sidebar .nav-link.active {
      background-color: #4e73df;
      color: white !important;
      font-weight: 600;
    }

    /* ------ MAIN CONTENT ------ */
    .content {
      margin-left: 240px;
      padding: 1.8rem;
      min-height: 100vh;
    }

    .page-title {
      font-size: 1.6rem;
      font-weight: 700;
    }

    /* ------ NAVBAR SUPERIOR ------ */
    .topbar {
      height: 60px;
      background: var(--bs-body-bg);
      border-bottom: 1px solid var(--bs-border-color);
      display: flex;
      justify-content: flex-end;
      align-items: center;
      padding: 0 1.5rem;
      position: fixed;
      left: 240px;
      right: 0;
      top: 0;
      z-index: 10;
    }

    /* ------ TARJETA ------ */
    .card {
      border: none;
      border-radius: 0.7rem;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.07);
      transition: background-color .3s;
    }

    /* ------ TABLA ------ */
    .table thead th {
      background-color: var(--bs-tertiary-bg);
      font-size: 0.78rem;
      text-transform: uppercase;
      letter-spacing: .04em;
    }

    .table tbody tr:hover {
      background-color: rgba(80, 80, 255, 0.06);
      transition: background-color .2s;
    }

    /* ------ SWITCH MODO OSCURO ------ */
    .switch-theme {
      display: flex;
      align-items: center;
      gap: 8px;
      cursor: pointer;
      font-size: 1rem;
    }

    footer {
      margin-left: 0px;
    }
    /* Estilos adicionales */
    main {
      flex: 1; /* Para que el main ocupe el espacio restante */
    }

    .navbar {
      background-color: var(--bs-body-bg) !important;
      transition: background-color .3s ease;
      border-bottom: 1px solid var(--bs-border-color);
    }

    .navbar-brand {
      font-weight: bold;
    }

    .nav-link {
      color: var(--bs-body-color) !important;
    }

    .navbar-text {
      color: var(--bs-body-color) !important;
    }

    footer {
      background-color: var(--bs-dark-bg-subtle) !important;
      color: var(--bg-secondary) !important;
      border-top: 1px solid var(--bs-border-color);
      transition: background-color .3s ease, color .3s ease, border-color 0.3s ease;
    }


  </style>
</head>

<body class="d-flex flex-column min-vh-100">

  <!-- NAVBAR SUPERIOR -->
  <nav class="navbar navbar-expand-lg navbar-light border-bottom">
    <div class="container">

      <!-- Logo + Nombre -->
      <a class="navbar-brand d-flex align-items-center gap-2" href="/">
        <img src="/img/logo.png" alt="USTA" style="height:32px; width:auto;" class="rounded">
        <span>PDS-001</span>
      </a>


      <!-- Botón colapsable móvil -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar"
        aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <!-- Contenido navbar -->
      <div class="collapse navbar-collapse" id="mainNavbar">

        <!-- Ítems del menú -->
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/equipos">Equipos</a></li>
          <li class="nav-item"><a class="nav-link" href="/ubicaciones">Ubicaciones</a></li>
          <li class="nav-item"><a class="nav-link" href="/responsables-custodios">Responsables</a></li>
          <li class="nav-item"><a class="nav-link" href="/personas-mantenimiento">Personas</a></li>
          <li class="nav-item"><a class="nav-link" href="/cargos">Cargos</a></li>
          <li class="nav-item"><a class="nav-link" href="/mantenimientos">Mantenimientos</a></li>
          <li class="nav-item"><a class="nav-link" href="/usuarios">Usuarios</a></li>
          <li class="nav-item"><a class="nav-link" href="/mi-perfil"><i class="bi bi-person me-1"></i> Mi perfil</a>
          </li>
        </ul>
          <div class="switch-theme">
    <input type="checkbox" id="themeToggle">
    <label for="themeToggle" class="m-0">🌙</label>
  </div>
        <!-- Usuario + Salir -->
        <% if (currentUser) { %>
          <span class="navbar-text me-3 text-muted small">
            Usuario: <strong>
              <%= currentUser.username %>
            </strong> |
            Rol: <strong>
              <%= currentUser.role %>
            </strong>
          </span>
          <form method="post" action="/logout" class="d-inline m-0">
            <button class="btn btn-outline-danger btn-sm">Salir</button>
          </form>
          <% } %>

      </div>
    
    </div>
  </nav>


  <!-- CONTENIDO PRINCIPAL -->
  <main class="flex-grow-1">
    <div class="container py-4">

      <!-- Título de la página -->
      <header class="mb-4 pb-2 border-bottom">
        <h1 class="h5 m-0">
          Módulo de gestión de inventario y registro de mantenimientos de equipos informáticos HSRT
        </h1>
      </header>

      <!-- FILA 1 -->
      <div class="row g-3 mb-2">
        <div class="col-12 col-sm-6 col-lg-4">
          <div class="card h-100 border-0 shadow-sm">
            <div class="card-body">
              <div class="d-flex align-items-center mb-2">
                <div
                  class="rounded-circle bg-success-subtle text-success fw-bold d-inline-flex align-items-center justify-content-center me-2"
                  style="width:40px;height:40px">📦</div>
                <h5 class="m-0">Equipos</h5>
              </div>
              <p class="text-secondary mb-3">Series, estados y mantenimiento.</p>
              <a href="/equipos" class="btn btn-success w-100">Entrar</a>
            </div>
          </div>
        </div>

        <div class="col-12 col-sm-6 col-lg-4">
          <div class="card h-100 border-0 shadow-sm">
            <div class="card-body">
              <div class="d-flex align-items-center mb-2">
                <div
                  class="rounded-circle bg-info-subtle text-info fw-bold d-inline-flex align-items-center justify-content-center me-2"
                  style="width:40px;height:40px">📍</div>
                <h5 class="m-0">Ubicaciones</h5>
              </div>
              <p class="text-secondary mb-3">Sedes, áreas, oficinas y salas.</p>
              <a href="/ubicaciones" class="btn btn-info w-100 text-white">Entrar</a>
            </div>
          </div>
        </div>

        <div class="col-12 col-sm-6 col-lg-4">
          <div class="card h-100 border-0 shadow-sm">
            <div class="card-body">
              <div class="d-flex align-items-center mb-2">
                <div
                  class="rounded-circle bg-warning-subtle text-warning fw-bold d-inline-flex align-items-center justify-content-center me-2"
                  style="width:40px;height:40px">👤</div>
                <h5 class="m-0">Responsables/Custodios</h5>
              </div>
              <p class="text-secondary mb-3">Asignaciones y custodias.</p>
              <a href="/responsables-custodios" class="btn btn-warning w-100">Entrar</a>
            </div>
          </div>
        </div>
      </div>

      <!-- FILA 2 -->
      <div class="row g-3 mb-2 justify-content-center">

        <!-- CARGOS -->
        <div class="col-12 col-sm-6 col-lg-4">
          <div class="card h-100 border-0 shadow-sm">
            <div class="card-body">
              <div class="d-flex align-items-center mb-2">
                <div
                  class="rounded-circle bg-primary-subtle text-primary fw-bold d-inline-flex align-items-center justify-content-center me-2"
                  style="width:40px;height:40px">💼</div>
                <h5 class="m-0">Cargos</h5>
              </div>
              <p class="text-secondary mb-3">Tipos de cargo (interno/externo) para personas de mantenimiento.</p>
              <a href="/cargos" class="btn btn-primary w-100">Entrar</a>
            </div>
          </div>
        </div>

        <!-- PERSONAS DE MANTENIMIENTO -->
        <div class="col-12 col-sm-6 col-lg-4">
          <div class="card h-100 border-0 shadow-sm">
            <div class="card-body">
              <div class="d-flex align-items-center mb-2">
                <div
                  class="rounded-circle bg-secondary-subtle text-secondary fw-bold d-inline-flex align-items-center justify-content-center me-2"
                  style="width:40px;height:40px">👥</div>
                <h5 class="m-0">Personas de mantenimiento</h5>
              </div>
              <p class="text-secondary mb-3">Técnicos/proveedores internos y externos a cargo de los equipos.</p>
              <a href="/personas-mantenimiento" class="btn btn-secondary w-100">Entrar</a>
            </div>
          </div>
        </div>

        <!-- MANTENIMIENTOS -->
        <div class="col-12 col-sm-6 col-lg-4">
          <div class="card h-100 border-0 shadow-sm">
            <div class="card-body">
              <div class="d-flex align-items-center mb-2">
                <div
                  class="rounded-circle bg-danger-subtle text-danger fw-bold d-inline-flex align-items-center justify-content-center me-2"
                  style="width:40px;height:40px">🛠️</div>
                <h5 class="m-0">Mantenimientos</h5>
              </div>
              <p class="text-secondary mb-3">Gestión de mantenimientos preventivos, correctivos y alertas.</p>
              <a href="/mantenimientos" class="btn btn-danger w-100">Entrar</a>
            </div>
          </div>
        </div>
      </div>

    </div>
  </main>

  <!-- FOOTER "FIJO" ABAJO -->
   <footer class="mt-auto py-2 border-top bg-light text-center text-body-secondary small">
    © <%= new Date().getFullYear() %> USTA – Módulo de gestión de inventario y registro de mantenimientos de equipos
      informáticos HSRT
  </footer>


  

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <script>
    const toggle = document.getElementById("themeToggle");
    const root = document.documentElement;

    const saved = localStorage.getItem("theme");
    if (saved) {
      root.setAttribute("data-bs-theme", saved);
      toggle.checked = saved === "dark";
    }

    toggle.addEventListener("change", () => {
      const theme = toggle.checked ? "dark" : "light";
      root.setAttribute("data-bs-theme", theme);
      localStorage.setItem("theme", theme);
    });
  </script>
</body>

</html>
